[project]
name = "remora-cli"
version = "3.0.0"
description = "Download any video/audio you want from a simple URL ✨"
dependencies = ["remora", "typer", "rich"]

[project.scripts]
remora = "remora_cli:run"

[build-system]
requires = ["uv_build>=0.8.0,<0.9"]
build-backend = "uv_build"
